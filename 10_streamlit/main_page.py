import streamlit as st # 외부 라이브러리를 사용하다보면 별칭이 존재함

# Streamlit은 서버를 재시작하지 않아도 동적 페이지라서 바로 바로 반영됨...
st.title('오늘은 목요일🌳🌲🪓')
st.header('오늘은 Streamlit 배우는 날!!!')
st.subheader('Streamlit으로 나만의 데모 페이지를 만들어 보자!')

my_site = st.text_input('오늘 내가 만들어보고 싶은 페이지는? ')
if my_site :
    # print(my_site) # 이거는 터미널에 출력됨.
    st.write(my_site) # 이렇게 해야 streamlit 화면에 출력됨.

    if st.button(f'{my_site} 접속하기') :
        st.success(f'{my_site} 접속 중 🚀🚀🚀')
